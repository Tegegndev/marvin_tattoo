# Marvin Tattoos Atelier Frontend Configuration
# Leave blank for local development / same-domain cPanel proxy (e.g. /api)
# Or set to your backend URL when hosted separately (e.g. https://api.yourdomain.com)
VITE_API_URL=
